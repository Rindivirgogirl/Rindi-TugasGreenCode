const Angka = [2, 4, 6, 8, 10]

for (let index = 2; index <= 10; index+=2) {
   
    if (Angka[index] % 2 == 0) {
        console.log([Angka] + " adalah bilangan genap")
    } else if (index!+=2) {
        
        
    }
    
}